language: node_js
sudo: false

node_js:
  - 16
  - 12
  - 10
  - 8

os:
  - linux

cache:
  directories:
    - $HOME/.npm

script:
  - npm test -- -t0 --reporter=spec --no-coverage --no-check-coverage

notifications:
  email: false
